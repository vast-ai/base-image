## Fooocus (this image)

Runs Fooocus, a streamlined image-generation UI (supervisor service `fooocus`).
It appears in `/capabilities/services`; reach it via `direct_url` + token. Not an
OpenAI endpoint.
