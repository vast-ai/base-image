## SwarmUI (this image)

Runs SwarmUI, a multi-backend Stable Diffusion UI (supervisor service `swarmui`).
It appears in `/capabilities/services`; reach it via `direct_url` + token. Not an
OpenAI endpoint.
