## ComfyUI (this image)

Runs ComfyUI, a node-based diffusion workflow UI (supervisor service `comfyui`),
plus an `api-wrapper` service for running saved workflows programmatically. Both
appear in `/capabilities/services`; reach them via `direct_url` + token. Not an
OpenAI endpoint.
