# TensorFlow derivative — image identity (overrides the base image block).
image:
  name: TensorFlow
  readme: https://github.com/vast-ai/base-image/tree/main/derivatives/tensorflow
  preinstalled: TensorFlow + CUDA in /venv/main
