## SGLang (this image)

Runs SGLang as an OpenAI-compatible server — service label **"SGLang API"**.

- For the externally callable `base_url` + auth: `curl -s http://localhost:11111/capabilities/endpoints`.
