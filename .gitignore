__pycache__
*.egg-info/
.env*
!.env.example

# template_manager run logs (may reference account ids; never commit)
tools/template_manager/output/
.qa/
.venv/

# uv lockfile for the imagegen tool venv — a local build artifact, not a pin we ship
tools/imagegen/uv.lock

# A second working copy someone dropped in the tree; never part of the image.
pcl/
