"""Declarative provisioner module.

Replaces ad-hoc bash provisioning scripts with a YAML manifest
that declares apt/pip packages, git repos, model downloads,
and supervisor services.
"""

__version__ = "1.0.0"
